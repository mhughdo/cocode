# testdata

This directory holds local fixtures used by backend integration tests, frontend seeded states, and fake CLI agents.

Planned fixtures:

- `repos/`: golden repositories for auth, webhook, and generated-file scenarios.
- `fake-agents/`: deterministic CLI scripts that emit valid, malformed, slow, or empty review outputs.
